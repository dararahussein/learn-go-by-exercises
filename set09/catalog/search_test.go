package catalog

import "testing"

func TestSearch(t *testing.T) {
	// TODO: replace this with a table-driven test before implementing Search.
	t.Fatal("write Search tests first: title, author, case, absent, empty")
}
